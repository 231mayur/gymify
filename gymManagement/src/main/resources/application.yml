spring:
  application:
    name: GymManagement
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
  datasource:
    url: jdbc:mysql://localhost:3306/gym
    username: root
    password: Mayur@10
server:
  port: 9090

